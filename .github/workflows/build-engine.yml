name: Build Agent Engine

on:
  push:
    branches: [main-rke2-mcp]
    paths:
      - "apps/engine/**"
      - ".github/workflows/build-engine.yml"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: apps/engine
          push: true
          tags: |
            ghcr.io/achi-456/agentic-engine:0.12.0
            ghcr.io/achi-456/agentic-engine:${{ github.sha }}

      - name: Update agent engine image tag in GitOps manifest
        run: |
          sed -i "s|ghcr.io/achi-456/agentic-engine:.*|ghcr.io/achi-456/agentic-engine:${{ github.sha }}|g" \
            k8s/apps/agentic-agents/deployment.yaml

      - name: Commit and push GitOps manifest update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/apps/agentic-agents/deployment.yaml
          git diff --staged --quiet && exit 0
          git commit -m "ci: update agent-engine image to ${{ github.sha }} [skip ci]"
          for attempt in 1 2 3; do
            git pull --rebase origin main-rke2-mcp
            git push origin HEAD:main-rke2-mcp && exit 0
            sleep 5
          done
          exit 1
