apiVersion: v1
kind: Service
metadata:
  name: agent-engine
  namespace: agentic-agents
spec:
  selector:
    app: agent-engine
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP

